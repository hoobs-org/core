#!/usr/bin/env node

process.title = "hoobs-upgrade";

const File = require("fs");
const Path = require("path");

require(Path.join(Path.dirname(File.realpathSync(__filename)), "../scripts/nginx"))(false);
